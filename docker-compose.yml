services:
  mongodb:
    image: mongodb/mongodb-community-server:7.0-ubuntu2204
    container_name: fitgrad-mongodb
    command: ["mongod", "--replSet", "rs0", "--bind_ip_all"]
    ports:
      - "27017:27017"

volumes:
  mongodb_data:
